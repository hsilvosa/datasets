[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "open-food-facts-historical"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pyarrow>=18"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools]
py-modules = []
